---
 - hosts: all
   tasks:
   - name: Set EPEL repo
     yum: name=epel-release state=present

   - name: Install Linux packages
     yum: name={{ item }} state=present
     with_items:
      - openldap
      - openldap-servers

   - name: Install Python packages
    pip: name=ldap3 state=present
